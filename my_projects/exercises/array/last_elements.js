function lastElements (array, n){ //show last n elements from array
    var i = 0; 
    for (i=array.length-n; i < array.length; i++){
    console.log (array[i]);
    }
    //console.log(last(array,n);
    }
    